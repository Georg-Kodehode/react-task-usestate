import { HeaderWrapper, HeaderTitle } from "./headerComponents";

const Header = () => {
  return (
    <HeaderWrapper>
      <HeaderTitle>React Hooks Task</HeaderTitle>
    </HeaderWrapper>
  );
};

export default Header;
